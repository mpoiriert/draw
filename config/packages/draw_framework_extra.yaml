draw_framework_extra:
  configuration: ~

  logger:
    slow_request:
      default_duration: 5000
      request_matchers:
        - path: '^/api'

  messenger:
    broker:
      receivers: [ 'async' ]
    doctrine_message_bus_hook:
      enabled: true

  versioning: ~
