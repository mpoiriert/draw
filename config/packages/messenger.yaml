framework:
    messenger:
        failure_transport: 'failed'

        transports:
            async: 'draw://default?queue_name=default'
            failed: 'draw://default?queue_name=failed'
            sync: 'sync://'
