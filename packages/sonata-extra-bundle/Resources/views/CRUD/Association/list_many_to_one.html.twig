{% extends get_admin_template('base_list_field', admin.code) %}

{% import '@DrawSonataExtra/CRUD/Association/macro_links.html.twig' as macros %}

{% block field %}
    {% if value %}
        {{ macros.association_links(field_description, value, value|render_relation_element(field_description), []) }}
    {% endif %}
{% endblock %}
