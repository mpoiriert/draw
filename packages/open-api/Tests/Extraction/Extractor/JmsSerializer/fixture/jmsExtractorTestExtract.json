{
  "swagger": "2.0",
  "definitions": {
    "Draw.Component.OpenApi.Tests.Extraction.Extractor.JmsSerializer.JmsExtractorStubModel": {
      "properties": {
        "virtual": {
          "description": "The virtual property.",
          "readOnly": true,
          "$ref": "#\/definitions\/Draw.Component.OpenApi.Tests.Extraction.Extractor.JmsSerializer.JmsExtractorStubModel"
        },
        "name": {
          "description": "The name.",
          "type": "string",
          "readOnly": true
        },
        "generic": {
          "type": "object",
          "properties": {
            "name": {
              "description": "The generic property.",
              "type": "string",
              "readOnly": true
            }
          },
          "readOnly": true
        },
        "backed_enum_int": {
          "description": "The backed enum int.",
          "enum": [
            1,
            2
          ],
          "type": "integer"
        },
        "backed_enum_name": {
          "description": "The backed enum by name.",
          "enum": [
            "ABC",
            "DEF"
          ],
          "type": "string"
        },
        "array_backed_enum_name": {
          "description": "Array be backed enum by name.",
          "items": {
            "enum": [
              "ABC",
              "DEF"
            ],
            "type": "string"
          },
          "type": "array"
        },
        "backed_enum": {
          "description": "The backed enum.",
          "enum": [
            "abc",
            "def"
          ],
          "type": "string"
        },
        "generic_enum": {
          "description": "The simple enum.",
          "enum": [
            "FOO",
            "BAR"
          ],
          "type": "string"
        },
        "serializeProperty": {
          "description": "Serialized property.",
          "type": "string"
        },
        "array": {
          "description": "The array.",
          "type": "array",
          "items": {
            "$ref": "#\/definitions\/Draw.Component.OpenApi.Tests.Extraction.Extractor.JmsSerializer.JmsExtractorStubModel"
          }
        }
      }
    }
  }
}
