{% extends '@SonataAdmin/standard_layout.html.twig' %}

{% block content %}
    <div>
        <h4>{{ 'form.enable_2fa.header' | trans([], 'DrawUserBundle') }}</h4>
        <p>{{ 'form.enable_2fa.description' | trans([], 'DrawUserBundle') }}</p>
        {{ qrCodeSvg|raw }}
        <br>
        <p><b>{{ 'form.enable_2fa.totp_label' | trans([], 'DrawUserBundle') }}:</b> {{ totpSecret }}</p>

        {{ form(form) }}
    </div>
{% endblock %}